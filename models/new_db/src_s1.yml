version: 2
sources:
  - name: globalmart
    database: my_db
    schema: my_schema
    tables:
      - name: first_testtable
        columns:
          - name: ID
            tests:
              - unique
              